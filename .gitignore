node_modules/
.next/
out/
build/
.DS_Store
*.pem
.env*
!.env.example
next-env.d.ts
.vercel
*.tsbuildinfo

# Local data left over from the old server build: a dev session secret and the
# JSON store. Nothing reads it any more, but it must never be committed.
.data/
